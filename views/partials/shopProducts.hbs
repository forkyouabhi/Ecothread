<div class="row row-cols-1 row-cols-md-3 g-4 productsList">
  {{#each this.products as |product|}}
  <div class="col">
    <div class="card">
      <img src="{{product.imageUrl}}" class="card-img-top" alt="{{product.name}}" width="10%">
      <div class="card-body">
        <h5 class="card-title">{{product.name}}</h5>
        <p class="card-text">{{product.description}}</p>
      </div>
       <div class="card-footer"> 
        {{!-- <small class="price">$ {{product.price}}</small> --}}
        <button productId="{{product._id}}" class="btn btn-primary addToCart">Add To Cart</button>
        <button type="button" productId={{product._id}} class="btn btn-primary addToCart" aria-label="Add {{product.name}} to cart">Add To Cart</button>
      </div>
    </div>
  </div>
  {{else}}
  <div class="col-12 text-center py-5">
    <div class="mb-3">
      <span style="font-size: 3rem;" aria-hidden="true">🛍️</span>
    </div>
    <h4>No products found</h4>
    <p class="text-muted">We don't have any products available right now. Please check back later!</p>
  <div class="col-12 text-center py-5 w-100">
    <div class="text-muted mb-3">
      <svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" fill="currentColor" class="bi bi-box-seam" viewBox="0 0 16 16" aria-hidden="true">
        <path d="M8.186 1.113a.5.5 0 0 0-.372 0L1.846 3.5l2.404.961L10.404 2l-2.218-.887zm3.564 1.426L5.596 5 8 5.961 14.154 3.5l-2.404-.961zm3.25 1.7-6.5 2.6v7.922l6.5-2.6V4.24zM7.5 14.762V6.84l-6.5-2.6v7.922l6.5 2.6zM5.22 4.243l-2.404-.961L8 1.113l2.404.961L5.22 4.243z"/>
      </svg>
    </div>
    <h4>No products available</h4>
    <p class="text-muted">Please check back later for new arrivals.</p>
  </div>
  {{/each}}
</div>
